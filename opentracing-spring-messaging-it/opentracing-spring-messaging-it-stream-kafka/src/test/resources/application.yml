spring:
  kafka:
    consumer:
      group-id: testGroup
  cloud:
    stream:
      bindings:
        input:
          destination: testDestination
          group: testGroup
        output:
          destination: testDestination
